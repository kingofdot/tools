# 결과물

실제로 생성된 갑지 예시를 넣는 자리입니다. **아직 없습니다.**

가이드(`갑지_이용가이드.png`)의 `05 결과물` 섹션도 "준비 중"으로 비워 두었습니다.
예시 파일을 이 폴더에 넣은 뒤, `_guide_gapzi.html` 의 05 섹션을 이미지 컷으로 바꾸고
`python _build_guide.py` 를 다시 돌리면 가이드에 반영됩니다.
